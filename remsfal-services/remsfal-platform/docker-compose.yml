services:
  postgres:
    image: postgres:16
    container_name: postgres
    environment:
      - POSTGRES_DB=REMSFAL
      - POSTGRES_USER=remsfaladmin
      - POSTGRES_PASSWORD=remsfalSecret
    ports:
      - "5432:5432"
