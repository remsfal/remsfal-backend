<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                                       http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                                       http://www.liquibase.org/xml/ns/dbchangelog-ext
                                       http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <changeSet id="remsfal-ticketing-0.0.1-issues" author="alexander.stanik@htw-berlin.de">
        <comment>Create issues table</comment>
        <sql>
            CREATE TABLE IF NOT EXISTS REMSFAL.issues (
                project_id UUID,
                issue_id UUID,
                type TEXT,
                title TEXT,
                status TEXT,
                reporter_id UUID,
                tenancy_id UUID,
                owner_id UUID,
                description TEXT,
                blocked_by UUID,
                related_to UUID,
                duplicate_of UUID,
                created_by UUID,
                created_at TIMESTAMP,
                modified_at TIMESTAMP,
                PRIMARY KEY (project_id, issue_id)
            ) WITH CLUSTERING ORDER BY (issue_id ASC);
        </sql>
        <sql>
            CREATE INDEX IF NOT EXISTS issues_issue_id_idx ON REMSFAL.issues (issue_id);
        </sql>
        <rollback>
            <sql>DROP TABLE IF EXISTS REMSFAL.issues;</sql>
            <sql>DROP INDEX IF EXISTS issues_issue_id_idx;</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>