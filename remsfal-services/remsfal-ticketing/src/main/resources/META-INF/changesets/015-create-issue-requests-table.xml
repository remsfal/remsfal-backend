<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                                       http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                                       http://www.liquibase.org/xml/ns/dbchangelog-ext
                                       http://www.liquibase.org/xml/ns/dbchangelog-ext.xsd">

    <changeSet id="remsfal-ticketing-015-issue-requests" author="Philipp.Bogatchev@Student.HTW-Berlin.de">
        <comment>Create issue requests table for requests a contractor sends to the tenant about an
        issue (e.g. asking for additional images)</comment>
        <sql>
            CREATE TABLE IF NOT EXISTS REMSFAL.issue_requests (
                issue_id UUID,
                issue_request_id UUID,
                organization_id UUID,
                agreement_id UUID,
                message TEXT,
                attachment_ids LIST&lt;UUID&gt;,
                created_at TIMESTAMP,
                modified_at TIMESTAMP,
                PRIMARY KEY (issue_id, issue_request_id)
            ) WITH CLUSTERING ORDER BY (issue_request_id ASC);
        </sql>
        <sql>
            CREATE CUSTOM INDEX IF NOT EXISTS issue_requests_organization_id_sai
            ON REMSFAL.issue_requests (organization_id)
            USING 'StorageAttachedIndex';
        </sql>
        <sql>
            CREATE CUSTOM INDEX IF NOT EXISTS issue_requests_agreement_id_sai
            ON REMSFAL.issue_requests (agreement_id)
            USING 'StorageAttachedIndex';
        </sql>
        <rollback>
            <sql>DROP TABLE IF EXISTS REMSFAL.issue_requests;</sql>
            <sql>DROP INDEX IF EXISTS REMSFAL.issue_requests_organization_id_sai;</sql>
            <sql>DROP INDEX IF EXISTS REMSFAL.issue_requests_agreement_id_sai;</sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
