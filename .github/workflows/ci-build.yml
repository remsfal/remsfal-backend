name: "CI Build"

on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  run-tests:
    name: JDK ${{ matrix.java }} Build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: [17, 21, 25] # LTS-Versions
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Build REMSFAL
        uses: ./.github/actions/build-remsfal
        with:
          java-version: ${{ matrix.java }}
      - name: Archive test output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports-jdk-${{ matrix.java }}-${{ github.sha }}
          path: |
            remsfal-core/target/surefire-reports/
            remsfal-services/*/target/surefire-reports/
          retention-days: 14
      - name: Build JaCoCo Reports (only on JDK 21)
        if: ${{ matrix.java == 21 }}
        run: mvn -B verify
      - name: Upload Coverage Reports (only on JDK 21)
        if: ${{ matrix.java == 21 }}
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ github.sha }}
          path: target/site/jacoco/jacoco.xml
          retention-days: 1
          if-no-files-found: error
      - name: Build Checkstyle Reports (only on JDK 21)
        if: ${{ matrix.java == 21 }}
        run: mvn -B checkstyle:checkstyle-aggregate
      - name: Upload Checkstyle Reports (only on JDK 21)
        if: ${{ matrix.java == 21 }}
        uses: actions/upload-artifact@v4
        with:
          name: checkstyle-${{ github.sha }}
          path: /target/checkstyle-result.xml
          retention-days: 1
          if-no-files-found: error

  check-branches:
    name: "Check Pull Request Conditions"
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Check PR branches
        run: |
          if [ "${{ github.head_ref }}" == "main" ] && [ "${{ github.base_ref }}" == "main" ]; then
            echo "Pull Requests to main branch are only allowed from developer branches that are not named <main>."
            exit 1
          fi
